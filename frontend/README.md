# Frontend

React + Vite interface for the Malayalam summarizer.

## Run

```bash
cd frontend
npm install
npm run dev
```

The app calls `http://127.0.0.1:8000` by default. To override it, create a Vite env variable:

```bash
VITE_API_URL=http://127.0.0.1:8000 npm run dev
```
